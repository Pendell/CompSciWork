To compile:

Navigate inside the /src/ folder within the ACO directory. At the terminal enter, "javac -d bin src/*.java && java -classpath bin EntColony".
This will compile all the necessary executables and run them.
