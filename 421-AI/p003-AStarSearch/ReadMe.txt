To compile:

Navigate inside the /src/ folder within the AStar directory. At the terminal enter, "javac *.java".
This will compile all of the necessary executables to run AStar.

After, enter "java AStarSearch" at the terminal and follow any additional prompts to run the program.