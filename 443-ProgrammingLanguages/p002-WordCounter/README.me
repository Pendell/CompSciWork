READ ME:

To build the application,

enter:
    $ gradle build
    
Then to run the application
    $ gradle run -P clArgs=&&&
    
replace the three ampersands with the directory of the file you want to parse through.
