To run the program in your command line, type

$ scheme --load p003.scm

to run the games while at the command line, type

> (in-range? x y z)

where x is the test number, y is the low, z is the high.

>(guess-my-number a b)

where a is the low end, and b is the high end. 